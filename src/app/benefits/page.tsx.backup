import type { Metadata } from 'next';
import Link from 'next/link';
import Heatmap from '@/components/Heatmap';
import { Reveal, Stagger } from '@/components/Reveal';

export const metadata: Metadata = {
  title: 'Benefits — Students, Teachers, Admin',
  description: 'See how precision diagnostics create visible growth for students, empower teachers, and enable data-driven schools.',
};

export default function BenefitsPage() {
  return (
    <div className="bg-white text-neutral-900 dark:bg-neutral-950 dark:text-white">
      <section className="border-b border-neutral-200/60 px-4 py-16 dark:border-neutral-800">
        <div className="mx-auto max-w-7xl">
          <Reveal>
            <h1 className="text-4xl font-extrabold tracking-tight">Benefits</h1>
          </Reveal>
          <Reveal delay={0.08}>
            <p className="mt-3 text-neutral-700 dark:text-neutral-300">Our diagnostic-led approach delivers clarity and impact for every stakeholder.</p>
          </Reveal>
        </div>
      </section>

      {/* Students */}
      <section className="mx-auto max-w-7xl px-4 py-16">
        <h2 className="text-3xl font-bold">What Students Gain</h2>
        <Stagger className="mt-8 grid gap-6 md:grid-cols-2 lg:grid-cols-4">
          {[
            { title: 'Clear Roadmap', desc: 'Precise view of strengths and areas for development.' },
            { title: 'Self‑Awareness & Motivation', desc: 'Own the why behind struggles and build resilience.' },
            { title: 'Visible Growth & Confidence', desc: 'Tangible progress fuels belief and momentum.' },
            { title: 'Reduced Exam Fear', desc: 'Concept clarity and targeted practice reduce anxiety.' },
          ].map((i) => (
            <div key={i.title} className="rounded-xl border border-neutral-200 p-6 shadow-sm ring-1 ring-transparent transition hover:shadow-lg hover:ring-emerald-500/20 dark:border-neutral-800">
              <h3 className="text-base font-semibold">{i.title}</h3>
              <p className="mt-2 text-sm text-neutral-600 dark:text-neutral-400">{i.desc}</p>
            </div>
          ))}
        </Stagger>
      </section>

      {/* Teachers */}
      <section className="bg-neutral-50 py-16 dark:bg-neutral-900">
        <div className="mx-auto max-w-7xl px-4">
          <h2 className="text-3xl font-bold">What Teachers Gain</h2>
          <Stagger className="mt-8 grid gap-6 md:grid-cols-3">
            {[
              { title: 'Class‑level heatmaps of weaknesses', desc: 'Instant clarity on class‑wide misunderstandings.' },
              { title: 'Plan targeted interventions', desc: 'Use precise data to tailor support where it matters.' },
              { title: 'Save time, maximize outcomes', desc: 'Automations reduce admin; focus on effective instruction.' },
            ].map((i) => (
              <div key={i.title} className="rounded-xl border border-neutral-200 p-6 shadow-sm ring-1 ring-transparent transition hover:shadow-lg hover:ring-emerald-500/20 dark:border-neutral-800">
                <h3 className="text-base font-semibold">{i.title}</h3>
                <p className="mt-2 text-sm text-neutral-600 dark:text-neutral-400">{i.desc}</p>
              </div>
            ))}
          </Stagger>

          <Reveal>
            <div className="mt-10 rounded-xl border border-neutral-200 bg-white p-6 shadow-lg shadow-emerald-900/5 ring-1 ring-emerald-500/10 dark:border-neutral-800 dark:bg-neutral-950">
              <Heatmap title="Class Weakness Heatmap (Demo)" />
              <p className="mt-3 text-xs text-neutral-500">Demo data. Integrate with your analytics backend to render real class heatmaps.</p>
            </div>
          </Reveal>
        </div>
      </section>

      {/* Admin */}
      <section className="mx-auto max-w-7xl px-4 py-16">
        <h2 className="text-3xl font-bold">What Admin Gains</h2>
        <Stagger className="mt-8 grid gap-6 md:grid-cols-4">
          {[
            { title: 'Comprehensive Data Visibility', desc: 'Granular and system‑wide insights to inform decisions.' },
            { title: 'Targeted Interventions & Efficiency', desc: 'Optimize resources with data‑driven action.' },
            { title: 'Enhanced Teaching Effectiveness', desc: 'Objective feedback for professional development.' },
            { title: 'Strengthened School Reputation', desc: 'Showcase commitment to personalized, data‑driven learning.' },
          ].map((i) => (
            <div key={i.title} className="rounded-xl border border-neutral-200 p-6 shadow-sm ring-1 ring-transparent transition hover:shadow-lg hover:ring-emerald-500/20 dark:border-neutral-800">
              <h3 className="text-base font-semibold">{i.title}</h3>
              <p className="mt-2 text-sm text-neutral-600 dark:text-neutral-400">{i.desc}</p>
            </div>
          ))}
        </Stagger>
      </section>

      {/* CTA */}
      <section className="mx-auto max-w-7xl px-4 pb-24">
        <div className="rounded-2xl bg-gradient-to-r from-emerald-600 to-teal-600 p-10 text-white shadow-xl shadow-emerald-900/10">
          <h3 className="text-2xl font-bold">Build a future‑ready institution.</h3>
          <p className="mt-2 opacity-90">Move from mark sheets to measurable growth with precision diagnostics.</p>
          <div className="mt-6 flex gap-3">
            <Link href="/contact" className="rounded-full bg-white px-5 py-3 text-emerald-700 shadow hover:opacity-95">Talk to us</Link>
            <Link href="/product" className="rounded-full border border-white/70 px-5 py-3 text-white hover:bg-white/10">Explore product</Link>
          </div>
        </div>
      </section>
    </div>
  );
}
